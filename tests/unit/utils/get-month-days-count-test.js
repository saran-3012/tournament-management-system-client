import getMonthDaysCount from 'tournament-management-system/utils/get-month-days-count';
import { module, test } from 'qunit';

module('Unit | Utility | get month days count');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getMonthDaysCount();
  assert.ok(result);
});
