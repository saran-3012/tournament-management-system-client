import tournamentImageFallback from 'tournament-management-system/utils/tournament-image-fallback';
import { module, test } from 'qunit';

module('Unit | Utility | tournament image fallback');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = tournamentImageFallback();
  assert.ok(result);
});
