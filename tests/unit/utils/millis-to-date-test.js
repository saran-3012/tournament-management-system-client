import millisToDate from 'tournament-management-system/utils/millis-to-date';
import { module, test } from 'qunit';

module('Unit | Utility | millis to date');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = millisToDate();
  assert.ok(result);
});
