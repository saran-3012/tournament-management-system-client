import tournamentPosters from 'tournament-management-system/utils/tournament-posters';
import { module, test } from 'qunit';

module('Unit | Utility | tournament posters');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = tournamentPosters();
  assert.ok(result);
});
