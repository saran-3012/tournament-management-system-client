import isLeapYear from 'tournament-management-system/utils/is-leap-year';
import { module, test } from 'qunit';

module('Unit | Utility | is leap year');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isLeapYear();
  assert.ok(result);
});
