import controllableInterval from 'tournament-management-system/utils/controllable-interval';
import { module, test } from 'qunit';

module('Unit | Utility | controllable interval');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = controllableInterval();
  assert.ok(result);
});
