import millisToTime from 'tournament-management-system/utils/millis-to-time';
import { module, test } from 'qunit';

module('Unit | Utility | millis to time');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = millisToTime();
  assert.ok(result);
});
