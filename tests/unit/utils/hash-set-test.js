import hashSet from 'tournament-management-system/utils/hash-set';
import { module, test } from 'qunit';

module('Unit | Utility | hash set');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = hashSet();
  assert.ok(result);
});
