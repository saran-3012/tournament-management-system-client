import controllableTimeout from 'tournament-management-system/utils/controllable-timeout';
import { module, test } from 'qunit';

module('Unit | Utility | controllable timeout');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = controllableTimeout();
  assert.ok(result);
});
