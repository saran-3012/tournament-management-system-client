import delayCalls from 'tournament-management-system/utils/delay-calls';
import { module, test } from 'qunit';

module('Unit | Utility | delay calls');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = delayCalls();
  assert.ok(result);
});
