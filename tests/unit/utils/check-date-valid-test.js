import checkDateValid from 'tournament-management-system/utils/check-date-valid';
import { module, test } from 'qunit';

module('Unit | Utility | check date valid');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = checkDateValid();
  assert.ok(result);
});
