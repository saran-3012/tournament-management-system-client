import passwordValidator from 'tournament-management-system/utils/password-validator';
import { module, test } from 'qunit';

module('Unit | Utility | password validator');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = passwordValidator();
  assert.ok(result);
});
