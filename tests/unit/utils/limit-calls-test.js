import limitCalls from 'tournament-management-system/utils/limit-calls';
import { module, test } from 'qunit';

module('Unit | Utility | limit calls');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = limitCalls();
  assert.ok(result);
});
