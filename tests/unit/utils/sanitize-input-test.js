import sanitizeInput from 'tournament-management-system/utils/sanitize-input';
import { module, test } from 'qunit';

module('Unit | Utility | sanitize input');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = sanitizeInput();
  assert.ok(result);
});
