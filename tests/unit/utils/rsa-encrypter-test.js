import rsaEncrypter from 'tournament-management-system/utils/rsa-encrypter';
import { module, test } from 'qunit';

module('Unit | Utility | rsa encrypter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = rsaEncrypter();
  assert.ok(result);
});
