import checkCharactersPresent from 'tournament-management-system/utils/check-characters-present';
import { module, test } from 'qunit';

module('Unit | Utility | check characters present');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = checkCharactersPresent();
  assert.ok(result);
});
