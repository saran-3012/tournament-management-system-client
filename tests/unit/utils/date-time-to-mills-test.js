import dateTimeToMills from 'tournament-management-system/utils/date-time-to-mills';
import { module, test } from 'qunit';

module('Unit | Utility | date time to mills');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dateTimeToMills();
  assert.ok(result);
});
